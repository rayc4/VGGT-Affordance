name: CDM

## contact space
input_feats: 1  # read by CDM.__init__ (contact_dim); value currently inert

## scene backbone (PointTransformerSeg + linear head)
scene_model:
  name: 'PointTransformerSeg'
  num_points: ${task.dataset.num_points}
  pretrained_weight:   # read by CDM.__init__; loading is currently disabled
  freeze: false        # read by CDM.__init__
